import 'package:flutter/material.dart';

class SubtractScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Subtract Screen'),
      ),
      body: Center(
        child: Text('Content for the Subtract Screen'),
      ),
    );
  }
}
